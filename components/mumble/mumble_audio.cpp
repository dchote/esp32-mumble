#include "mumble_audio.h"

namespace esphome {
namespace mumble {

// Placeholder: no I2S or Opus integration yet.
// Will implement capture path (I2S -> AEC -> Opus encode) and
// playback path (UDP -> jitter buffer -> Opus decode -> I2S) in future.

}  // namespace mumble
}  // namespace esphome
